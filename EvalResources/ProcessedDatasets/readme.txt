In this folder, we have all the datasets required for training and validating the system:

The First sets will be used to train models able to detect DoS, Probing and are trained on samples of regular
traffic as well.

KDDTrain_l1.txt, KDDVal_l1.txt -> Dos and Probe
KDDTrain_l2.txt, KDDVal_l2.txt -> U2R and R2L